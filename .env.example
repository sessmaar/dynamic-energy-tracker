# Copy to .env (repo root) and to apps/web/.env.local for Next.
# Mobile picks up these values from the workspace root via packages/data.

# --- Supabase ---------------------------------------------------------------
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.replace_me

# Next.js (web) needs the NEXT_PUBLIC_ prefix to expose values to the browser
NEXT_PUBLIC_SUPABASE_URL=${SUPABASE_URL}
NEXT_PUBLIC_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

# Expo (mobile) needs the EXPO_PUBLIC_ prefix
EXPO_PUBLIC_SUPABASE_URL=${SUPABASE_URL}
EXPO_PUBLIC_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
