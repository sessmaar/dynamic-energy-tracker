# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
build
.next
.expo
.expo-shared
.metro-cache
*.tsbuildinfo

# Test / coverage
coverage
.nyc_output

# Env — Supabase URL is public but anon key + service role must NEVER ship.
# We commit .env.example (see README) but never the real values.
.env
.env.local
.env.*.local
*.env

# OS / editor
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
expo-debug.log*

# Expo / credentials
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# iOS / Android native dirs (when EAS Build adds them later)
ios/Pods
ios/build
android/.gradle
android/build
android/app/build
android/.idea
android/local.properties

# Generated tarballs
*.tgz

# macOS / external-drive artifacts
.Spotlight-V100
.Trashes
.fseventsd
